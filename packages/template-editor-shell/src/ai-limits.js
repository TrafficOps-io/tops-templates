// A run can contain several provider calls, file edits and validations.
export const AI_RUN_TIMEOUT_MS = 15 * 60 * 1000;
export const AI_STEP_TIMEOUT_MS = 5 * 60 * 1000;
export const AI_INITIAL_SOURCE_BYTES = 64 * 1024;
